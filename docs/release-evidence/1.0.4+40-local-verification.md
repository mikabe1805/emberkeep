# Room of Days 1.0.4+40 — local release verification

Prepared September 1, 2026 from the isolated Build 40 worktree on
`feature/build40-classes-widgets-focus`. This is a local candidate record, not
an Apple upload receipt and not authorization for public App Review.

## Decision state

| State | Verdict | Evidence boundary |
| --- | --- | --- |
| `code_complete` | local pass | All 1,095 Flutter tests pass, analysis is clean, Android debug and web release builds succeed, and the iOS source has been reviewed. WidgetKit compilation and signing require macOS. |
| `visual_evidence_ready` | bounded pass | Import, Focus, What's New, and the owner-selected Open Door of Light have fresh normal, compact, mask, and shipping-output evidence. The final App Store set is held for a real signed widget capture. |
| `owner_device_accepted` | open | The adaptive icon is verified in an Android emulator, but physical-iPhone file handoff, reminders, widget behavior, accessibility, audio feel, physical-launcher feel, and upgrade feel remain unaccepted. |
| TestFlight artifact | absent | No Build 40 upload has been made. |
| App Store review | held | The screenshot manifest remains truthfully bound to Build 39. |

## Apple capability preparation

On September 1, 2026, the Apple Developer account for Team `D63Z4RBRT8` was
prepared for the signed widget build:

- App Group `group.com.mikabe.emberkeep` was registered;
- widget App ID `com.mikabe.emberkeep.DayLedgerWidget` was registered;
- the App Group was assigned to both `com.mikabe.emberkeep` and
  `com.mikabe.emberkeep.DayLedgerWidget`;
- Apple warned that the capability changes invalidate older provisioning
  profiles. No existing profile is treated as evidence for Build 40. The exact
  tagged cloud build must create or fetch fresh App Store profiles for both
  targets and verify the group in each signed bundle and embedded profile.

This account preparation is not an archive, upload, processing receipt, or
device acceptance.

App Store Connect was also checked before freezing the candidate: Build 39 was
the live iOS high-water mark, and the intended internal tester group was `Me`.
The Build 40 workflow names that exact group in post-processing. This observation
does not predict whether Apple will accept or finish processing Build 40.

## Candidate scope

- review-first native `.ics` handoff plus a programmer-editable starter;
- opt-in class reminders with preserved re-import behavior;
- small and medium iPhone Day Ledger widgets for the next class and up to three
  unfinished Quests, using a bounded private App Group projection;
- session-scoped Fable room music in Focus with a one-tap quiet control;
- Build 40 in-app notes, store copy, TestFlight instructions, privacy copy, and
  a device-gated screenshot story;
- owner-selected Open Door of Light launcher identity, propagated through the
  deterministic iOS, Android, web, Windows, maskable, and themed-icon pipeline.

## Local gates

- `flutter test --no-pub`: 1,095 passed. The final run also pinned the golden
  pointer state after a full-suite-only hover transition exposed a nondeterministic
  button frame; the reference artwork itself was not changed.
- `flutter analyze --no-pub`: no issues.
- `flutter build apk --debug --no-pub`: passed.
- `flutter build web --release --no-pub`: passed; Wasm dry run passed.
- What’s New capture test: seven passed with fresh 430x932 and 320x568 at 2x
  text evidence.
- Store verifier: field limits, URLs, release identity, in-app notes, privacy,
  health, export, and license checks pass. The only remaining failure is the
  intentional Build 39 screenshot-manifest mismatch.
- Icon pipeline: fourteen focused export, manifest, review, and platform-asset
  tests pass; the final shipping sheet was opened at original resolution and
  preserves the threshold at 32 pixels and across platform masks.
- Android launcher: the fresh debug APK was installed on an Android emulator;
  the labeled adaptive icon was inspected beside Room Notes in the app drawer
  and remains distinct, centered, unclipped, and legible at launcher size.

## Required Apple and owner pass

1. Compile and archive the exact candidate on macOS with fresh exact profiles
   for both targets and both App Group entitlements provisioned.
2. Install over the previous TestFlight build and as a fresh install.
3. Open and share representative `.ics` files from Files, Mail, and another
   provider, both cold and warm; confirm review always precedes saving.
4. Exercise reminder off, on, offset changes, permission denial, and re-import.
5. Add both widget sizes; verify empty, populated, completed-Quest, class-end,
   lock-screen redaction, locale, background, and relaunch behavior.
6. Listen to Focus on speaker and headphones with global music both off and on;
   confirm one-tap quiet and preference restoration.
7. Check Largest Text, VoiceOver, Reduced Motion, lower brightness, thumb feel,
   and warm-device performance.
8. Inspect the exact Open Door of Light output at Home Screen and App Library
   size on a physical iPhone, plus legacy, adaptive, and themed launchers on a
   physical Android device. The emulator adaptive-launcher pass is recorded.
9. Capture the real Home Screen widget, recapture the complete ten-frame store
   story, inspect it, then create the Build 40 manifest-only binding receipt.

Until all nine steps pass, Build 40 is a strong local candidate rather than an
installable or review-ready App Store release.
