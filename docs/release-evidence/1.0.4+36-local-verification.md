# Room of Days 1.0.4+36 — local release verification

Prepared August 30, 2026 from the isolated candidate on
`release/room-of-days-1.0.4-build-36`. This record authorizes one internal
TestFlight build from the immutable receipt described below. It does not claim
that a signed iPhone build has been felt and accepted, and it does not authorize
a public App Store review submission before that gate.

## Decision state

| State | Verdict | Evidence boundary |
| --- | --- | --- |
| `code_complete` | pass | Candidate behavior, copy, release identity, automated app/backend checks, and web compilation are complete. |
| `visual_evidence_ready` | pass | Ten fresh 1290×2796 production-geometry App Store frames were exported and inspected individually. |
| `owner_device_accepted` | open | No physical iPhone is connected to this Windows host; owner feel, signed Apple services, two identities, and device accessibility remain unproven. |
| TestFlight artifact | pending | Codemagic must reproduce the gates, sign the IPA, upload Build 36, and emit the artifact receipt. |
| App Store review | held | Public submission waits for the processed Build 36 artifact and explicit owner/device acceptance. |

## Included release scope

- Goals now open into the living room, shape an honest route in the Workshop,
  and create the first Quest only after explicit acceptance. Recovery can make
  a difficult day smaller without mutating a route behind the person's back,
  and repeatable Quests grow into visible mastery.
- Material sound belongs to the accepted outcome rather than pointer contact.
  Scrolling, pointer cancellation, and inactive controls stay quiet.
- A Circle refresh now distinguishes “not found” from “could not check.” A
  transient failure retains the saved relationship and the last good room card,
  explains that the Circle is still there, and offers Retry.
- Removing a Circle space uses an accessible 44-point action and a confirmation
  that names the loss of mutual-page access before cleanup begins.
- The App Store packet now tells the actual product story across Quests, Reward,
  Goals, Workshop, Recovery, Plans, My Space, Change Space, Journal, and
  Discover. The listing, in-app What's New, TestFlight copy, and What to Test
  instructions all identify Build 36.

## Local gates passed

- `flutter analyze --no-pub`: no issues.
- `flutter test --no-pub`: all 944 Flutter tests passed.
- Focused social/Circle packet: 46 tests passed; focused analysis was clean.
- Pressable accepted-action packet: 6 tests passed; cancellation and no-op
  controls produced neither activation nor sound.
- Feature-on discovery/profile packet: 24 tests passed with one intentional
  feature assertion skipped.
- Functions lint and TypeScript build passed; all 110 Functions unit tests
  passed.
- Firestore emulator authorization suite: all 12 tests passed. Its denied-write
  warnings are the expected negative authorization cases.
- Feature-on release web build completed successfully. Its Wasm dry-run and icon
  tree-shaking notices were non-blocking Flutter build information.
- The screenshot exporter produced ten opaque RGB PNGs at exactly 1290×2796.
  Every frame was inspected at its production composition; awkward interim
  Reward, Journal, and empty-stat fixtures were rejected and recaptured.
- Repository whitespace checks passed. The generated Windows plugin files were
  hash-identical to the candidate base and are not part of the release diff.

## Known dependency maintenance item

`npm audit --omit=dev` reports nine moderate entries, all fanning out from one
transitive `uuid <11.1.1` advisory under the current Google/Firebase server SDK
tree. The affected API requires a caller-provided output buffer in UUID v3, v5,
or v6. Room of Days does not import `uuid` or expose those buffer arguments, and
this candidate makes no Functions dependency or deployment change. npm's only
automatic proposal is a forced breaking downgrade of Firebase Admin, so the
candidate leaves the tested production tree intact and records the upstream
upgrade as maintenance work. This is not represented as a clean dependency
audit.

## Immutable receipt protocol

1. Commit every Build 36 source, test, listing, documentation, and screenshot
   change while the candidate manifest remains an unfilled template.
2. Record that candidate commit as the manifest's `sourceRevision`, add the
   lowercase SHA-256 of each of the ten screenshots, and create a second commit
   changing only `store-assets/screenshots/app-store/CANDIDATE-MANIFEST.json`.
3. On the clean receipt commit, the iOS packet verifier must prove that the
   manifest names `1.0.4+36`, its source revision is the immediate parent,
   `HEAD^..HEAD` contains only the manifest, and every screenshot name,
   dimension, opacity, and hash matches.
4. The exact tag `room-of-days-1.0.4-build-36-candidate` must point to that
   receipt commit. No branch push or similarly named tag may trigger the iOS
   workflow.

## Physical TestFlight acceptance still required

After Apple processes Build 36, install it over the current build and as a fresh
install. The owner pass must cover the complete first-goal, returning-goal,
Workshop acceptance, difficult-day recovery, repeatable mastery, scroll/sound,
largest-text, VoiceOver, Reduced Motion, Ring/Silent, native handoff, relaunch,
and warm-performance journeys.

The two-identity pass must prove private and listed states, Discover opt-in and
opt-out, one-way Circle, reciprocal Mutuals, per-card audiences, offline Circle
retry without card loss, confirmed removal, report, block, code rotation, and a
positive signed App Attest exchange. `owner_device_accepted` stays open until
that exact Build 36 artifact looks good, feels good, and behaves cohesively on
the phone.

Only after those passes should the App Store draft receive the Build 36
screenshots and copy, select the Build 36 binary, and enter another public review
request. Codemagic's `release-evidence.txt` beside the IPA remains the authority
for the signed artifact; this document establishes the local pre-upload gate.
