# Non-secret, project-specific release gates. Public names are enabled only
# behind real App Check; deploys must never weaken the first line in production.
DISCOVERY_ENFORCE_APP_CHECK=true
DISCOVERY_PUBLIC_NAMES_ENABLED=true
PLACES_ENFORCE_APP_CHECK=false
