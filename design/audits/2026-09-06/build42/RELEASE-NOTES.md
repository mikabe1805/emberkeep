# Room of Days 1.0.4+42 — TestFlight candidate

The owner selected Refined and authorized a TestFlight build on 2026-09-06:

> go for refined! and then build it and upload it to testflight and after i test it let's upload it to the app store. maybe take a look and improve workshop while youre at it if it still exists in the app

## Candidate scope

- Includes the previously selected authored Goals/Quests experience: today's three, optional recommended Quests, review before accepting a new goal action, and manageable-step adjustments.
- Installs the exact selected Refined masters: 20 ordinary, 27 material, and 60 paired-return cues. The original paired musical accents and completion masters are preserved. The tap integration receipt and immutable snapshot manifests live in the adjacent `tap-refinement` audit directory.
- Includes the approved original Lamp left on composition and the previously verified main-music rotation, Focus separation, and web gesture retry.
- Restores a persistent Workshop entrance on Goals, including when the focused goal already has a Quest and when no goals exist. Visiting and leaving preserve today's three and the save's goal/Quest state.
- Keeps the Steward's existing room and optional scene. The invitation uses darker paper, the originating goal is marked, and each register entry states its next action. Registered page presses use the shared Refined sound director.
- Fixes a QuestCard lifecycle defect found by the release suite: an untouched card could initialize its animation during disposal. The controller is now initialized while its State is active; regression coverage removes an untouched reduced-motion card before and after motion-setting changes.
- Adds truthful Build 42 release notes while retaining Build 41 as historical content. Runner and WidgetKit both identify the new build.

## Evidence and limits

Fresh Goals and Workshop renders were inspected at 430 pixels and at 320 pixels with enlarged text. The register is captured with mixed work and an empty bench. The first-Quest acceptance, cancellation, existing-Quest handoff, optional conversation, and today-selection contracts remain covered by the interaction suite. Images are in `workshop/`; the current release-card renders are in `whats-new/`.

The owner chose the Refined audition; its output device was not recorded. Installed iPhone sound/feel and release acceptance remain the owner's TestFlight check. Public App Store submission is explicitly disabled in the build workflow and awaits that test. Local verification and exact source/receipt identities are recorded with the final delivery receipt outside the source candidate.

Historical Build 41 tags and receipts remain unchanged. Build 42 uses a new exact tag and a manifest-only child commit binding it to the frozen source. The cloud workflow independently rechecks the latest Apple build number before archiving, signs Runner and WidgetKit, verifies IPA contents, and publishes only to TestFlight's existing Me group.
