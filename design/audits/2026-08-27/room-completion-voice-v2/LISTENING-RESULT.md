# LISTENING-RESULT — room-completion-voice-v2

**Status: `longer-settle` approved — shipped to runtime byte-identical.**

Device: owner's phone, via the `completion-audition` Firebase preview
channel (2026-08-27). Exact model unstated.

- `control` (as shipped): superseded
- `polish`: not chosen
- `deeper-seat`: not chosen
- `weighted-answer`: not chosen
- `longer-settle`: **approved**

Owner verdict (verbatim):

> the longer settle sounds good!

Same message also directed the next study: "but make sure there's also an
actually longer celebratory sound for actual achievements like level ups"
— spawned `room-levelup-ceremony-v1`.
