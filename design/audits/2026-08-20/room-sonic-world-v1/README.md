# One Room system proof

Status: **desktop cohesion and semantic separation passed; Answered detent won
the focused reward follow-up; two physical-phone passes rejected the everyday
family.** The focused v2 correction still sounded unfinished in isolation and
only became fun during rapid tapping. Nothing in this folder is wired into the
app. See `PHONE-LISTENING-RESULT.md` and
`../room-phone-click-v2/LISTENING-RESULT.md`.

The prior semantic set was technically clean but failed as a family. This study
compares the exact same interaction timeline in two forms:

- **previous mixed study** — approved C navigation, the previous open/select
  experiments, the generic save cue, and the current completion cue, calibrated
  to the same semantic level ladder as the candidate;
- **unified Room family** — every role inherits one C-derived contact, one
  modal body bank, one provisional D-major-pentatonic phrase state, and one
  feed-forward early-reflection fingerprint.

The first browser pass is energy controlled and identity-blind. It asks only:

1. Which sequence feels more like one Room, if either?
2. Do its open, select, navigate, place, and complete gestures remain distinct?

Only after that verdict does the lab reveal identities and expose isolated
roles, the exact C anchor, shared contact/body lineage, a true reflection-only
room-bus bypass, and matched rapid alternating runs. Those are diagnosis, not
the approval unit.

## Shared render contract

- Contact master: `approved-c-contact-v1`
- Body master: `room-modal-bank-v1`
- Space fingerprint: `room-close-reflection-v1`
- Variant axis: global phrase token only (`D`, `E`, or `A` root family)
- Completion: one accepted-state select contact, then the related bloom exactly
  75 ms later as one composed event
- Reference-video audio used: **no**
- Runtime assets changed: **no**

`manifest.json` records the source graph and exact timeline. `sonic-system.json`
is the durable product contract. `SONIC-WORLD.md` explains the direction in
human terms.

## Verification

The authoring test checks deterministic renders, exact compound timing,
48 kHz/24-bit mono output, clipping, level-control matching, shared derivation
IDs, and the short feed-forward space fingerprint. The first physical iPhone
listening gate did not pass: the ordinary family now needs a focused texture
and level revision while the selected reward remains locked.

## Listening outcome

The blind level-controlled pass preferred the unified candidate and the verbs
remained distinct. The first natural hierarchy pass preferred the previous
mixed study because its reward carried more satisfying weight. The focused
follow-up kept every ordinary role unchanged; Answered detent then won both its
matched tournament round and the natural comparison against the previous
reward: `../room-reward-voice-v1/`.
