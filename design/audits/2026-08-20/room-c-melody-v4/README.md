# Room of Days melodic X v4

Focused physical-phone audition after `room-c-gesture-v3`.

## Decision carried forward

The v3 challenger X is the approved ordinary physical gesture. Its complete
60 ms contact, body, 8.5 ms clasp, `+1.5 dB` phone lift, role weights, and all
source hashes are locked. Answered Detent remains the locked completion at
`+75 ms`.

Owner evidence, verbatim:

> sounds really great but can you make the clicking melodic variations sound better overall when clicking around?

This is specific approval of X's physical gesture and a request to revise only
the variation across a sequence.

## Study axis

Both candidates use:

- the exact same locked X source takes;
- the same five short D-centered note stems;
- the same note multiset, level, cadence, roles, and completion;
- one global phrase state across controls, reset after 850 ms of quiet;
- melody on only the first four events of a rapid burst.

Only note order differs:

- `paired-return`: D5, A5, E5, D5, A4, E5, F#5, D5;
- `gentle-arc`: D5, E5, F#5, A5, E5, D5, A4, D5.

The modal answer begins at 10.5 ms, after X's clasp, and is approximately
10 dB quieter than the ordinary X body on the phone-weighted measure. It uses
no room bus and parks inside the original 60 ms cue boundary.

## Phone protocol

1. Use the iPhone built-in speaker at full volume with Bluetooth disconnected.
2. Replay plain X only as a fixed reference.
3. For both hidden choices, finish the single, slow eight-click walk, and Room
   click-around flow. Rapid playback is optional and does not unlock a choice.
4. Choose A, B, or neither. Neither means keep plain X without this melodic
   lane.
5. Confirm the selected phrase with a single click, the one/two/three-click
   pause probe, and the rapid stress run.

The browser exports the hidden mapping, exact heard paths, natural completion
events, device/route attestations, and the listener's reason verbatim. No
runtime assets are changed until this physical-device gate passes.

## Result

`paired-return` won the contour comparison, but the owner preferred plain X
with slight physical variation as the everyday sound and described melody as
a fun easter egg. The recorded browser was Windows Chrome, so this establishes
the policy and motif preference without passing the physical-phone gate. The
subsequent `room-paired-return-phone-gate-v1` passed on an iPhone 17, approving
only its bounded four-note return. See `LISTENING-RESULT.md` and the corrected
gate's `LISTENING-RESULT.md`.
