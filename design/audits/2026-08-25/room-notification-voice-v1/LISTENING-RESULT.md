# LISTENING-RESULT — room-notification-voice-v1

**Status: `knock-paced` selected — advancing to the lock-screen gate.**

Device: owner's phone, via the hosted audition page
(`sound-audition` Firebase preview channel, 2026-08-25). Exact model unstated.

- `knock-once`: not chosen
- `knock-paced`: **selected**
- `knock-settle`: not chosen
- `field-call`: not chosen

Owner verdict (verbatim):

> i think two knocks rising

Note on confidence: the pick is hedged ("i think"), which is exactly what the
remaining gate is for. This verdict advances `knock-paced` to a real
lock-screen delivery on the next TestFlight build; the sound only becomes
final when it earns approval there. The wired masters are byte-identical to
`candidates/knock-paced.wav` and byte-locked by test.
